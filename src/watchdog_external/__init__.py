"""
Внешний watchdog для maxtg_bridge.

Работает НЕ на том хосте, где живёт bridge: контейнер разворачивается на втором
VPS и наблюдает production-хост снаружи. Отсюда два жёстких правила:

1. Только стандартная библиотека. Контейнер не должен тянуть aiogram/pymax/aiohttp
   и не должен импортировать модули bridge: наблюдатель обязан переживать любую
   поломку наблюдаемого.
2. Никаких приватных данных. Watchdog видит только коды состояний и счётчики,
   которые отдаёт status API и `docker inspect`.

Модель отказов и раскладка по слоям — docs/runbooks/watchdog.md.
"""

SCHEMA_VERSION = 1
